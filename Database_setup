CREATE DATABASE load_balancer_db;
USE load_balancer_db;

CREATE TABLE servers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(50),
    current_load INT DEFAULT 0,
    max_capacity INT DEFAULT 100
);
